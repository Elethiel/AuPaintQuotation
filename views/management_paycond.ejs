<!--  ****************************  PAY COND   ****************************   -->
&nbsp;<br/>
&nbsp;<br/>

<div class="row text-center">
    <button type="button" id="addPayCond" class="btn btn-primary">Ajouter une Condition de Paiement</button>
</div>
&nbsp;<br/>
<small class="help">Les Conditions de Paiement sont utilisées pour les devis et facture comme accord sur la manière dont le client pourra s'acquitter de sa facture.</small><br/>
&nbsp;<br/>
<div class="row">
    <input type="hidden" id="maxPayCond" value="<%= srv.payCondList.length %>" />
    <table id="PayCondtab" class="stripe hover cell-border row-border" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>#</th>
                <th>Condition de Paiement</th>
                <th>&nbsp;</th>
            </tr>
        </thead>
 
        <tbody>
            <% for(var i = 0; i < srv.payCondList.length; i++) { %>
                <tr>
                    <td id="idPayCond<%= i %>"><%= srv.payCondList[i].payCondId %></td>
                    <td id="labelPayCond<%= i %>"><%= srv.payCondList[i].payCondLabel %></td>
                    <td><a href="/payCond?payCondId=<%= srv.payCondList[i].payCondId %>"><span class="glyphicon glyphicon-pencil"></span></a> 
                        <a id="delPayCond<%= i %>" href="#<%= i %>"><span class="glyphicon glyphicon-remove text-danger"></span></a></td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>
&nbsp;<br/>
&nbsp;<br/>